# MediaSaver 环境变量配置
# 复制为 .env 使用

# 浏览器类型: auto | chromium | edge
BROWSER=auto

# 是否启用无头模式 (1=启用, 0=禁用)
HEADLESS=1

# Chromium 可执行文件路径（Linux 下默认 /usr/bin/chromium）
CHROMIUM_BIN=/usr/bin/chromium

# 服务端口
PORT=8932

# 速率限制: 每 IP 每分钟最大请求数
RATE_LIMIT=10
